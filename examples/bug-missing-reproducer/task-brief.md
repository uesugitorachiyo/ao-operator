# Bug Missing Reproducer Brief

Use AO Operator to fix the broken task search behavior.

This intentionally omits the required failure proof so the bug-fix shape gate
must block before any mutator dispatch.
