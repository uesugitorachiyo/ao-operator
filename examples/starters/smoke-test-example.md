# Smoke Test Example

Verify that `python3 scripts/factory_run.py --list-profiles` lists the public
profiles without requiring provider credentials. Capture the command output and
note any missing starter profile.
