# Documentation Update Example

Update the setup guide so a first-time user understands that provider
authentication uses local Codex or Claude CLI OAuth, not API keys. Keep the
change short and verify that the referenced commands still exist.
