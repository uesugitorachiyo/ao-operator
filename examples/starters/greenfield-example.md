# Greenfield Example

Build a small CLI command named `plainfactory-slug` that converts a title into a
lowercase dash-separated slug. It should accept one positional title, print the
slug, and include focused tests for spaces, punctuation, and repeated dashes.
