# Agent OS Mission Router State Sample

Build the first Agent OS mission-router and state snapshot foundation.

Shape: greenfield.

Acceptance criteria:

- Route a brief into classification, shape, route labels, blocker state, and
  required verification.
- Produce a state snapshot that names the required project artifacts.
- Do not dispatch AO providers.
- Do not change existing AO Operator runtime routing behavior.
