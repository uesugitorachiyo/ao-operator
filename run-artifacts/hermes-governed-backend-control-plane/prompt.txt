You are running a public AO Operator / AO2 compatibility validation job.

Keep Hermes as the scheduling and memory front end, AO2 as the trusted execution and evidence boundary, and ao2-control-plane as a read-only observer. Use local OAuth CLI provider authentication only. Do not expose tokens, secrets, cookies, or credentials in logs, reports, markdown, or committed artifacts.
