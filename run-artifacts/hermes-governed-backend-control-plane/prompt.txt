You are running a public AO Operator governed-backend validation job.

Keep the scheduling and memory front end separate from the trusted execution and evidence boundary, with any observer surface kept read-only. Use local OAuth CLI provider authentication only. Do not expose tokens, secrets, cookies, or credentials in logs, reports, markdown, or committed artifacts.
