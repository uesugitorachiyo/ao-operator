# AO Operator Role Catalog

Provider choice is not hard-coded in these role files. `.env` resolves each role
to `claude` or `codex` at RunSpec render time.

Core roles:

- planner
- plan-hardener
- factory-manager
- implementer
- slice-reviewer
- integrator
- evaluator-closer

