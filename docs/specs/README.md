# Specs

Store task specs here. Each spec should include classification, shape, success
criteria, scoped reads/writes, negative constraints, and verification.

Generated specs should be committed with their matching plan, status artifacts,
and evaluator report. Do not commit isolated generated specs as baseline
evidence.
