# Plans

Store per-task hardened implementation plans here. Plans should be
execution-ready and map cleanly to AO DAG tasks.

The full AO Operator implementation SDD lives separately under `docs/sdd/`.

Generated plans should be committed with their matching spec, status artifacts,
and evaluator report. Do not commit isolated generated plans as baseline
evidence.
