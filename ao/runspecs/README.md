# AO RunSpecs

`ao-operator-smoke.yaml` is the static Codex-default smoke RunSpec.

For provider-aware rendering from `.env`, use:

```bash
python3 scripts/render_runspec.py --output /tmp/ao-operator-smoke.yaml
```

